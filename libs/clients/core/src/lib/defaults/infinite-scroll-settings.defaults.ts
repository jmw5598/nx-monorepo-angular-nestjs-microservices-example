import { InfiniteScrollSettings } from '../models';

export const defaultInfiniteScrollSettings: InfiniteScrollSettings = {
  throttle: 300,
  scrollUpDistance: 1,
  scrollDownDistance: 2
} as InfiniteScrollSettings;
